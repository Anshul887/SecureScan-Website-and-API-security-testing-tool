openapi: 3.0.0

info:
  title: SecureScan API
  version: 1.0.0

paths:

  /api/auth/login:
    post:
      summary: Login

  /api/projects:
    post:
      summary: Create Project

  /api/scans/start:
    post:
      summary: Start Scan
